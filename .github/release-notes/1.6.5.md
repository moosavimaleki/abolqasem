## رفع ساخت انتشار

- ساخت cross-platform sidecar اکنون CLI مستقل `cargo-zigbuild` را با subcommand درست `build` اجرا می‌کند.
- بررسی نصب ابزار از اجرای build جدا شده است؛ بنابراین مرحلهٔ بررسی ابزار دیگر با آرگومان نامعتبر شکست نمی‌خورد.
