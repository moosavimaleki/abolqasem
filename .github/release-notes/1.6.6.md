## رفع ساخت همهٔ پلتفرم‌ها

- workflow پیش از cross-build، targetهای Rust لازم برای Linux، macOS و Windows در معماری‌های amd64 و arm64 را نصب می‌کند.
- ساخت sidecar دیگر در نخستین target غیر-native با خطای نبودن crate `core` متوقف نمی‌شود.
