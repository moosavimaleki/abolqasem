This directory is generated by `scripts/prepare-web-assets.sh`.

It exists so Go's `//go:embed all:web` has a stable directory during tests.
Do not commit built assets from this directory; `web-react/` is the source.
