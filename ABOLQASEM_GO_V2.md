خوبه در ui جدید هم ما این چیز ها رو بیاریم
یعنی انگار این چیز ها رو اوردیم توی abolqasem-go


—-----------
تسک۱
وضعیت: پیاده‌سازی شد

- لیست نشست‌ها را از دیسک و hookها می‌گرفت و در sidebar نشان می‌داد.
  در این پروژه هم بهید همچنان به hook ها گوش بدیم
  اینجا وب سوکت هم داریم و کار آسون تره
  شاید کسی داره توی tui کار میکنه و اون جا سشن رو میدریت میکنه و اینجا میخواد reader باشه
  بنابراین با hook ها خودمون رو بروز میکنیم
  اما ایا اینکه notif بدیم یا نه رو میزاریم توی تنظیمات http://127.0.0.1:9090/settings/general
  هر نشستی اپدیت میشه توی منوی نشت های sidebar هم طبیعتا odrer نشان دهنده بروز شدنش خواهد بود

توضیحات UI/UX تسک۱

- Sidebar باید همچنان نقطه‌ی اصلی دیدن پروژه‌ها و نشست‌ها باشد؛ یعنی نشست‌هایی که از hook یا discovery دیسک می‌آیند نباید یک UI جدا بسازند، بلکه باید در همان ساختار project group و chat row فعلی Abolqasem-Go ادغام شوند.
- هر نشست باید فقط یک row پایدار داشته باشد. کلید پایدار باید از agent/session id/path ساخته شود تا با هر hook یا scan دوباره، نشست تکراری در sidebar ساخته نشود.
- وقتی یک نشست بیرونی از TUI یا hook آپدیت شد، row همان نشست باید بدون پرش بصری در sidebar تازه شود و بر اساس `updated_at` به جای درست خودش منتقل شود. این جابه‌جایی باید نرم و قابل فهم باشد، نه اینکه کل sidebar چشمک بزند.
- اگر کاربر الان داخل همان نشست است، نباید toast مزاحم بگیرد و نباید از reader mode یا scroll position بیرون پرت شود. فقط transcript/read model همان نشست باید refresh شود و اگر کاربر نزدیک انتهای چت است auto-follow فعال شود.
- اگر کاربر داخل نشست دیگری است، بروزرسانی باید به شکل badge یا subtle indicator روی row نشست آپدیت‌شده دیده شود؛ مثلا یک نقطه‌ی کوچک، عبارت کوتاه «به‌روزرسانی شد»، یا highlight کوتاه چندثانیه‌ای.
- تنظیمات notification باید در General settings قرار بگیرد و بهتر است سه سطح داشته باشد: خاموش، فقط badge در sidebar، toast و صدا. مقدار پیش‌فرض بهتر است فقط badge باشد تا viewer مزاحم کار active نشود.
- در موبایل، notification نباید drawer/sidebar را خودکار باز کند. فقط badge روی دکمه‌ی sidebar یا row مربوطه کافی است.
- empty/loading state sidebar باید تفاوت «هنوز نشستی نداریم»، «در حال اسکن نشست‌های دیسک»، و «hook نصب نیست یا خاموش است» را واضح کند.
- هدف UX این تسک این است که کاربر حس کند Web UI همیشه با TUI همگام است، اما کنترل صفحه از دستش خارج نمی‌شود.


—-----------
تسک۲
وضعیت: پیاده‌سازی شد

- جستجو در همین نشست داشت.
- جستجوی global در متن همه‌ی نشست‌ها از backend داشت، نه فقط عنوان نشست‌ها.
  این دو ویژگی عالی هستن
- chip مربوط به scope جستجو داشت که با Backspace حذف می‌شد.

عالیه Abolqasem اصلی این ویژگی ها رو نداره و باید بهش اضافه بشه

توضیحات UI/UX تسک۲

- Search باید بخشی از تجربه‌ی اصلی sidebar/chat باشد، نه یک صفحه‌ی جدا. بهترین جای شروع، بالای sidebar یا command/search area نزدیک فهرست نشست‌هاست، چون نتیجه‌ی global هم در همان ناحیه‌ی نشست‌ها معنی پیدا می‌کند.
- وقتی کاربر داخل یک chat است، scope پیش‌فرض «همین نشست» باشد. این scope باید با یک chip واضح کنار input دیده شود تا کاربر بداند فقط transcript فعلی را می‌گردد.
- وقتی input خالی است و کاربر Backspace می‌زند، chip «همین نشست» حذف شود و scope به «همه نشست‌ها» تغییر کند. این رفتار باید با tooltip یا placeholder قابل کشف باشد.
- جستجوی همین نشست باید داخل transcript فعلی highlight بدهد، تعداد match را نشان دهد، و کنترل قبلی/بعدی داشته باشد. اگر transcript کامل لود نشده، UI باید صادقانه بگوید «در پیام‌های بارگذاری‌شده» یا از backend برای کل نشست استفاده کند.
- جستجوی همه نشست‌ها باید backend-driven باشد، با debounce حدود 250 تا 350 میلی‌ثانیه، pagination/infinite scroll، و cancellation برای queryهای قدیمی‌تر. UI نباید در حین search قفل شود.
- نتایج global بهتر است در sidebar با group پروژه نمایش داده شوند: نام پروژه، نام نشست، timestamp، provider، و ۱ تا ۳ snippet کوتاه با highlight عبارت جستجو.
- کلیک روی نتیجه باید chat را باز کند و اگر message id/line match داریم transcript را به همان پیام scroll کند و چند ثانیه highlight کند.
- اگر نتیجه‌ای پیدا نشد، empty state باید قطعی باشد: «نتیجه‌ای در متن نشست‌ها پیدا نشد»، نه متن loading دائمی.
- اگر backend search خطا داد، UI باید retry بدهد و query کاربر را حفظ کند.
- در حالت فارسی، placeholderها و empty stateها فارسی باشند؛ اما متن snippet باید جهت خودش را بر اساس همان الگوریتم تشخیص متن workspace بگیرد، نه صرفا زبان UI.
- هدف UX این تسک این است که sidebar از «لیست اخیر» به «مرکز بازیابی حافظه‌ی همه نشست‌ها» تبدیل شود.

—-----------
تسک۳
وضعیت: پیاده‌سازی شد

- reader mode برای پیام‌های assistant داشت.
- reader تنظیمات فونت، سایز، line-height، theme و عرض محتوا داشت.
- دکمه reader sticky/floating داشت که هنگام خروج دکمه‌ی اصلی از viewport کنار message-card می‌آمد.(با انیمیشن)
- Markdown/HTML پیام‌ها را تمیز render می‌کرد.
- Mermaid را render می‌کرد، نه فقط متن code block.
- برای Mermaid دکمه‌های copy، zoom in/out، reset، fullscreen و pan داشت.
- فونت‌های محلی مثل Vazir، B Zar و Iranian Sans داشت.


این هم خیلی مهمه و میتونیم بیاریم
نکته این هست که به خوبی rtl رو میشناخت
اما در این نرم فازار ما چند زبانه هستیم
در لیست چت ها و پیام های درون چت چطوری rtl رو بشناسیم
اغلبنرم افزار های شناسایی اتوماتیک دارن و به اولین کلمه پراگراف توجه میکنند
هر پاراگراف اگر با فارسی شروع بشه کلش رو rtl میکنند و اگر با انگلیسی کلش رو ltr و اگر بلاک کد باشه کلش رو ltr
اما من روش بهتری سراغ دارم
چگالی کاراکتر(یا چگالی لغت ، هرکدوم که پردازش کمتری داره)
اگر چگالی فارسی در یک پاراگراف زیاد بود حتی اگر بلاک <code> بود باید rtl باشه در غیر این صورت ltr است
این بهترین روش است
چون گاهی پاراگراف فارسی با لغت و کلمه انگلیسی شروع میشه
گاهی در بلاک های کد متن کاملا فارسی نوشته میشه
ربطی نداره که کاربر در مد فارسی سایت است یا در مد انگلیسی! این تشخیص بر اساس پاراگراف است و فقط برای بخش چت ها یعنی workspace هست
این چیزی که گفتم هم درون workspace هست هم درون reader mode که اینگونه شرایط رو داشته باشه

سایر بخش های uه از مد فارسی و انگلیسی زبان انتخاب شده تبعیت می کنند

توضیحات UI/UX تسک۳

- Reader mode باید مکمل transcript باشد، نه جایگزین کل chat. کاربر باید بتواند از روی هر پیام assistant وارد reader شود و با بستن reader دقیقا به همان جای transcript برگردد.
- دکمه reader باید فقط روی پیام‌هایی دیده شود که ارزش خواندن بلند دارند؛ مثلا assistant text بلند، markdown سنگین، answer دارای headings، یا محتوای Mermaid. برای پیام‌های کوتاه مزاحم نباشد.
- sticky/floating reader button باید با `jump-to-end` هم‌تراز باشد و وقتی دکمه‌ی اصلی از viewport خارج شد با انیمیشن نرم ظاهر شود. انیمیشن می‌تواند حس کشسان/جدا شدن داشته باشد، ولی نباید تمرکز خواندن را خراب کند.
- Reader settings باید از Settings عمومی جدا ولی هماهنگ باشد: فونت، سایز، line-height، theme، و عرض محتوا. جای مناسب آن داخل reader toolbar است، با امکان ذخیره در local storage یا app settings.
- renderer پیام، reader و file markdown route باید reusable باشد. هر قابلیت markdown، code block، Mermaid، table، linkify و RTL detection فقط در یک لایه‌ی shared پیاده شود تا با اضافه شدن فیچر به reader، chat و file route هم بهره ببرند.
- برای تشخیص جهت متن، معیار اصلی باید block/paragraph-level باشد. هر پاراگراف، list item، table cell، quote و حتی inline-code/code-comment می‌تواند بر اساس چگالی کاراکتر فارسی/عربی `rtl` یا `ltr` شود.
- code blockهای واقعی برنامه‌نویسی باید LTR باقی بمانند، اما اگر محتوای block عمدتا فارسی است، container متن باید RTL شود بدون اینکه ترتیب monospace و copy خراب شود.
- Mermaid باید مثل code block یک frame مستقل داشته باشد: toolbar فشرده، label، copy، zoom in/out، reset، fullscreen. روی نمودارهای بزرگ، حالت اولیه بهتر است fit-to-width باشد و wheel/pan فقط وقتی pointer روی canvas است فعال شود.
- fullscreen Mermaid باید context کاربر را حفظ کند؛ خروج از fullscreen نباید scroll transcript یا reader را جابه‌جا کند.
- code blockها باید header مشابه Mermaid داشته باشند: زبان، copy، و در صورت امکان wrap/nowrap. این باعث می‌شود چت‌ها از لحاظ بصری یک سیستم واحد داشته باشند.
- فونت‌های محلی فارسی مثل Vazir، B Zar و Iranian Sans باید فقط برای متن خواندنی استفاده شوند؛ UI shell خود Abolqasem می‌تواند font فعلی خودش را حفظ کند تا هویت Abolqasem از بین نرود.
- هدف UX این تسک این است که Web UI فقط محل چت کردن نباشد، بلکه برای مطالعه‌ی جواب‌های بلند، کد، مستندات و نمودارها هم قابل استفاده باشد.

—----------------

تسک4
وضعیت: پیاده‌سازی شد

فایل بروزر

- لینک‌های local file مثل http://127.0.0.1:9090/home/.../file.py:31 را می‌شناخت.
- preview فایل در modal داشت.
- route مستقیم فایل داشت.
- اگر فایل Markdown بود، در route خودش مثل reader رندر می‌شد، نه به شکل سورس.
- خط مشخص‌شده در فایل را highlight می‌کرد.
- برای امنیت، فقط loopback/local file preview را قبول می‌کرد.


فایل بروزر هم فیچر عالی هست که باید پیاده سازی بشه
منتها در نرم افزار قبلی ما روتر نداشتیم!
الان react ای که داریم استفاده میکنیم روتر درونی داره
بنا بر این تمام روت های درون رو باید با _ شروع کنیم که بفهمیم مال ری اکت هست! یعنی چی!

http://127.0.0.1:9090/chat/legacy-chat-c719f441973bdb47
تبدیل میشه به
http://127.0.0.1:9090/_/chat/legacy-chat-c719f441973bdb47


http://127.0.0.1:9090/settings/general
تبدیل میشه به
http://127.0.0.1:9090/_/settings/general

اینجوری اگر یهو دیدیم
http://127.0.0.1:9090/home/h-mousavi/Projects/Hamed/codex-rtl-plugin/scripts/dev-install.sh
متوجه میشیم که باید file browser عمل کنه
چون این لینک ها توسط کدکس و claude جنریت میشه و ما کنترلی روش نداریم!

توضیحات UI/UX تسک۴

- قانون route باید برای کاربر نامرئی ولی برای سیستم قطعی باشد: همه routeهای داخلی React با `/_/` شروع شوند و هر path خام روی root به عنوان file browser بررسی شود.
- لینک‌های تولیدشده توسط agentها نباید نیاز به rewrite دستی داشته باشند. اگر agent لینکی مثل `http://127.0.0.1:9090/home/.../file.py:31` داد، کلیک روی آن باید مستقیم file preview را باز کند.
- وقتی لینک فایل از داخل transcript یا reader کلیک می‌شود، بهتر است modal باز شود تا context چت حفظ شود. modal باید title فایل، path کوتاه‌شده، شماره خط، copy path و open in route داشته باشد.
- وقتی همان فایل به صورت direct route باز می‌شود، تجربه باید full page باشد. در این حالت close modal و open route معنی ندارد، چون کاربر همین حالا داخل route فایل است.
- برای فایل‌های markdown در direct route، خروجی باید با reader renderer نمایش داده شود، نه سورس خام. این route باید TOC، Mermaid، code toolbar، table، linkify و reader settings را reuse کند.
- برای markdown داخل modal، اگر context چت مهم‌تر است می‌توان preview را compact نگه داشت، اما مسیر direct باید تجربه‌ی کامل reader باشد.
- برای فایل‌های code، line highlight باید واضح ولی غیرتهاجمی باشد. صفحه باید بعد از load روی line هدف scroll کند و یک highlight چندثانیه‌ای داشته باشد.
- فایل browser باید breadcrumbs سبک داشته باشد تا کاربر بفهمد کجای filesystem است، اما نباید شبیه file manager کامل شود؛ scope این تسک preview/read است.
- خطاهای امنیتی باید human-readable باشند: «این مسیر مجاز نیست»، «فایل پیدا نشد»، «فایل باینری است»، «فایل برای preview بزرگ است». پیام خطا نباید pathهای حساس اضافه‌تر را لو بدهد.
- از نظر امنیت، frontend فقط تجربه را کنترل می‌کند؛ تصمیم نهایی باید در backend باشد. backend باید loopback بودن درخواست، allowlist مسیرها، اندازه فایل، نوع فایل و traversal را بررسی کند.
- مسیرهای internal مثل `/_/chat/...` و `/_/settings/general` نباید توسط file browser گرفته شوند. برعکس، مسیرهای خام مثل `/home/...` یا `/Users/...` نباید به React router داده شوند.
- هدف UX این تسک این است که لینک‌های فایل داخل جواب agent واقعا قابل استفاده شوند، بدون اینکه کاربر از context چت گم شود یا امنیت local server ضعیف شود.
ممکنه route ها حروف بزرگ و کوچک داشته باشن که در برخی سیتسم عامل ها مهمه
باید بتونه بک اند اون ها رو پارس کنه
